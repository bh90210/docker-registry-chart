apiVersion: v1
kind: Service
metadata:
  name: {{ include "registry-chart.fullname" . }}
  labels:
    {{- include "registry-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "registry-chart.selectorLabels" . | nindent 4 }}
